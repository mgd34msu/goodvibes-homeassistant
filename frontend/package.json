{
  "name": "goodvibes-homeassistant-frontend",
  "version": "0.0.0",
  "private": true,
  "description": "Build pipeline for the GoodVibes Home Assistant sidebar panel assets.",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "check": "node build.mjs --check"
  },
  "devDependencies": {
    "esbuild": "0.25.5"
  }
}
